const size = {
    mobile: '425px',
    tablet: '620px',
    desktop: '1024px',
};

const breakpoints = {
    mobile: `(min-width: ${size.mobile})`,
    tablet: `(min-width: ${size.tablet})`,
    desktop: `(min-width: ${size.desktop})`,
};

export { size, breakpoints };
